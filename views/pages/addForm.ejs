<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="/styles/style.css" type="text/css" rel="stylesheet"/>
        <title>Ajouter</title>
    </head>
    <body>
      <div id="container">
        <nav>
            <a href="/">Accueil</a>
            <a href="/viewPlan">List EDT</a>
        </nav>
        <h1>Formulaire</h1>
        <div>
          <form id="addForm">
            <% inputs.forEach(val=>{%>
              <div><label for=<%= val.label %> ><%= val.label %></label>
              <input type=<%= val.type %> id= <%= val.label %> id=<%= val.name %> required>
            </div><% })%>           
              <input type="submit" value="Enregistrer">
            </form>
          </div>
      </div>
    </body>
    </html>
    <script type="text/javascript" src="/utils/addService.js"></script>
